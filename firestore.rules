rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // DEV-ONLY. Do not ship.
    function tempOpen() {
      return request.time < timestamp.date(2026, 2, 1);
    }
    match /{document=**} {
      allow read, write: if tempOpen();
    }
  }
}
